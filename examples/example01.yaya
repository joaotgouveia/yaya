# Example 01

fn printInt(int i);

fn helloworld: int (int hello) {
   return hello;
}

fn main: int () {
    let int world = 0;
    let int hello = helloworld(helloworld(world));
    printInt(hello);
    hello = 1;
    printInt(hello);
    return 0;
}
